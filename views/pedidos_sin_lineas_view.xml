<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Vista lista -->
    <record id="view_pedidos_sin_lineas_list" model="ir.ui.view">
        <field name="name">pedidos.sin.lineas.list</field>
        <field name="model">rpt.pedidos_sin_lineas</field>
        <field name="arch" type="xml">
            <list string="Pedidos sin líneas">
                <header>
                    <button name="print_report"
                        type="object"
                        string="Descargar PDF"
                        class="btn-primary" />
                </header>
                <field name="id" />
                <field name="id_cliente" />
                <field name="fecha" />
                <field name="estado" />
                <field name="importe_base" />
                <field name="descuento" />
                <field name="iva" />
                <field name="importe_total" />
            </list>
        </field>
    </record>
</odoo>